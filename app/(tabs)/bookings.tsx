import { View, Text } from 'react-native';

import { Container } from '~/components/container';

const Bookings = () => {
  return (
    <Container>
      <Text>Bookings</Text>
    </Container>
  );
};

export default Bookings;
